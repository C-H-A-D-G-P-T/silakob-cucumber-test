{
	"default": {
		"require": ["src/hooks/**/*.ts", "src/steps/**/*.ts"],
		"requireModule": ["ts-node/register"],
		"formatOptions": {
			"snippetInterface": "async-await"
		},
		"publishQuiet": true,
		"timeout": 30000,
		"retry": 1
	}
}
